# 현재 진행상황 정리 (2026-01-28 기준)

## 1) 프론트 재스캐폴딩 완료
- Vite → Next.js 14(App Router) + TypeScript + Tailwind 전환
- 설정/파일 추가:
  - `frontend/next.config.js`
  - `frontend/tsconfig.json`
  - `frontend/next-env.d.ts`
  - `frontend/tailwind.config.js`
  - `frontend/postcss.config.js`
  - `frontend/.eslintrc.json`
  - `frontend/src/app/layout.tsx`
  - `frontend/src/app/page.tsx`
  - `frontend/src/app/globals.css`
- Vite 잔재 제거:
  - `frontend/vite.config.js`
  - `frontend/index.html`
  - `frontend/eslint.config.js`
  - `frontend/src/App.jsx`
  - `frontend/src/main.jsx`
  - `frontend/src/App.css`
  - `frontend/src/index.css`
  - `frontend/src/assets/react.svg`

## 2) Docker 통합 구성 완료 (Mac/Windows 호환 고려)
- `docker-compose.yml`에 `frontend`, `backend` 서비스 추가
- 폴링 기반 파일 감지 활성화:
  - `CHOKIDAR_USEPOLLING=true`
  - `WATCHPACK_POLLING=true`
- Dockerfile 추가:
  - `frontend/Dockerfile`
  - `backend/Dockerfile`
- Docker ignore 추가:
  - `frontend/.dockerignore`
  - `backend/.dockerignore`
- Neo4j 드라이버 추가: `backend/requirements.txt`
- `.env.example`의 기본 URI를 Docker 기준으로 반영

## 3) Day 1 작업 완료 (UI/구조 스켈레톤)
- 폴더 분리:
  - `frontend/src/components/scene`
  - `frontend/src/components/ui`
  - `frontend/src/store`
  - `frontend/src/lib`
- 전역 타이포/배경 톤 구성:
  - `frontend/src/app/layout.tsx`에 `Space Grotesk` 적용
  - `frontend/src/app/globals.css` 그라데이션 배경 추가
- 홈 레이아웃 확정:
  - `frontend/src/app/page.tsx`에 3D 씬 + 채팅 패널 2컬럼 구조 구성
- 3D 씬 placeholder:
  - `frontend/src/components/scene/SceneShell.tsx`
- 채팅 패널 placeholder:
  - `frontend/src/components/ui/ChatPanel.tsx`
- Zustand 스토어 스텁:
  - `frontend/src/store/useChatStore.ts`

## 4) Day 2 작업 완료 (3D 씬 스켈레톤)
- force-graph 3D 캔버스 구성:
  - `frontend/src/components/scene/GraphCanvas.tsx`
- 샘플 그래프 데이터 정의:
  - `frontend/src/lib/sampleGraph.ts`
- 카메라/라이트 기본 설정 및 force 레이아웃 동작 확인

## 5) Day 3 작업 완료 (렌더링/라벨 폴리시)
- 커스텀 노드 렌더링:
  - emissive 머티리얼 + 타입별 컬러/스케일 적용
  - 글로우 쉘(플레이스홀더) 추가
- 빌보드 라벨 스프라이트 적용 (캔버스 텍스처)
- 링크 소재/파티클 톤 정리

## 6) Day 4 작업 완료 (인터랙션/동기화)
- 노드 클릭 시 선택 상태 및 포커스 UI 표시
- 카메라 fly-to 애니메이션 추가 (노드 중심)
- Zustand로 3D ↔ 2D 상태 동기화
- 채팅 패널에 선택 노드/연결 노드 정보 반영
  - 참고: 현재 환경에서 카메라 fly-to 체감되지 않는 이슈 존재 (해결 보류)

## 7) Day 5 작업 완료 (2D 채팅 패널/플로우)
- 메시지 리스트/빈 상태/추천 프롬프트 UI 구성
- 입력 → 응답 흐름 연결 (샘플 응답/키워드 표시)
- Framer Motion으로 카드/메시지 애니메이션 적용

## 8) Day 6 작업 완료 (Backend & Intelligence)
- Gemini API 연동 및 응답 파싱 로직 추가
- 답변 + 키워드 추출(JSON 구조) 구현
- Neo4j 저장 로직 확장: `UserQuery -> Answer -> Keyword`
- `/api/chat` 응답 스키마 확장 (graph nodes/links 포함)
- `.env.example`에 Gemini 환경변수 추가

## 9) Day 7 작업 완료 (Integration & State)
- 프론트 ↔ 백엔드 `/api/chat` 연동 (fetch)
- 응답 데이터 기반 채팅 메시지/키워드 반영
- 그래프 merge 로직(Zustand) 추가로 레이아웃 유지
- 로딩/에러 상태 UI 처리 (typing indicator + 에러 배지)
- 3D 씬에서 store 그래프 데이터 동기화

## 10) Day 8 작업 진행 (Visual Polish)
- Bloom(Post-processing) 적용으로 발광 톤 강화
- 노드 머티리얼 재정의 (emissive/roughness/metalness 튜닝)
- 글로우/헤일로 레이어 추가로 깊이감 강화
- 엣지 파티클 속도/밀도 조정 + 라인 맥동 애니메이션
- 스타필드 + Fog로 배경 깊이 추가
- 씬 톤앤매너 그라데이션 보강

## 11) Day 9 작업 진행 (UX & Performance)
- 카메라/오빗 컨트롤 튜닝 (damping/rotate/zoom 거리 조정)
- 모바일 레이아웃 보정 (패딩/최소 높이/입력 폼 스택)
- 렌더링 성능 튜닝 (pixel ratio 제한, 파티클 수/속도 조정)

## 12) Day 10 작업 진행 (Demo & QA)
- README 실행 가이드 정리 (Docker/로컬)
- QA 체크리스트 확정 (Phase 4 문서 업데이트)
- 데모 마무리 준비 및 최종 점검 항목 정의

## 13) Day 10 QA 결과
- QA 체크리스트 전 항목 데스크탑 기준 완료
- fly-to 기능 비활성화(향후 개선 예정)

## 14) 이슈 대응 현황
- `react-force-graph-3d` 버전 오류 확인 → 최신 버전(1.29.0) 설치 안내
- Next App Router에서 이벤트 핸들러 오류 → `ChatPanel`에 `"use client";` 추가
- Neo4j 컨테이너 재시작 문제 → `docker-compose.yml`의 `neo4j` 서비스 `env_file` 제거로 해결
- Docker 접속 혼동: `docker compose build`는 빌드만 수행 → `docker compose up -d`로 실행 후 접속 필요

## 15) 일일 로그
### 2026-01-26
- Day 2: force-graph 3D 캔버스 + 샘플 그래프 + 카메라/라이트 세팅
- Day 3: 커스텀 노드/글로우/라벨 스프라이트 + 엣지/파티클 톤 정리
- Docker: Neo4j 컨테이너 재시작 이슈 해결 (`neo4j` 서비스 `env_file` 제거)
- 확인: 로컬 3D 씬 렌더링, `npm run lint` 통과

### 2026-01-27
- Day 4: 노드 클릭/선택 상태 + 카메라 fly-to + 3D↔2D 동기화
- 포커스 링 표시 및 채팅 패널 컨텍스트 동기화 추가
- 이슈: 카메라 fly-to 체감 안됨 → 다음 작업으로 이월
- Day 5: 2D 채팅 패널 입력/응답 플로우 + 메시지 애니메이션 적용
- Day 6: Gemini 연동 + 키워드 추출 + Neo4j 그래프 저장 + 응답 스키마 확장
- Day 7: 프론트 연동 + 그래프 merge + 로딩/에러 처리 + 3D 동기화

### 2026-01-28
- Day 8: Bloom + 머티리얼 폴리시 + 엣지 파티클/라인 애니메이션
- 스타필드 + Fog 추가로 공간감 강화
- 씬 배경 톤 보강
- Docker: 빌드 후 접속 불가 → `docker compose up -d` 실행 안내
- Day 9: 카메라/오빗 튜닝 + 모바일 레이아웃 보정 + 성능 튜닝
- Day 10: README 실행 가이드 + QA 체크리스트 업데이트
- Day 10: QA 전 항목 데스크탑 기준 확인 완료, fly-to 제거

## 16) 관련 문서
- [[MVP_타이트_개발_계획]]
- [[Phase 3 - 개발 (MVP)]]
- [[AI Chat 웹사이트 제작 그랜드 플랜]]

---
다음 작업: Day 10 — QA 체크리스트 검증 완료 + 버그 픽스  
추가 이슈: 카메라 fly-to 체감 문제 해결 이월
